# 문서 종류별 파이프라인 분리 이유 (Hidden 폰트 발견, 2026-08-14)

## 배경

`extract_compare_v4.py`(대아이앤씨 네이티브 PDF 검증) 작업 중, `needs_review`
비율이 34.9%나 나오는 원인을 `page.chars`로 직접 확인하다가 발견했다.
같은 이름("Hidden"이 들어간 폰트)이 문서 종류에 따라 **정반대의 의미**를
가진다는 걸 알게 됐고, 그래서 v3와 v4가 같은 필터링 로직을 공유하면 안 되는
이유가 됐다.

## v3(스캔본, `ocr_done/06-03_acrobat_ocr.pdf`)와 v4(네이티브,
`raw/대아이앤씨 건강보험 납부확인서 26.04.pdf`)의 차이

| | v3: 스캔본 (Acrobat GUI OCR) | v4: 네이티브 (공단 직접 발급 PDF) |
|---|---|---|
| 원본 상태 | 스캔 이미지, 텍스트 없음 | 애초에 진짜 텍스트로 생성됨 |
| 실제 본문 폰트 | 없음 (이미지만 있음) | `LKBAPO+Dotum` (임베드 서브셋 폰트) |
| `HiddenHorzOCR` 폰트의 정체 | Acrobat이 이미지를 OCR해서 얹은 **유일한 텍스트 레이어** | 본문과 별개로 겹쳐 삽입된 **품질 나쁜 중복 레이어** |
| 실측 비율 | 13번 페이지 기준 1462자 중 1147자(78%)가 HiddenHorzOCR | 1페이지 기준 정상 926자(LKBAPO+Dotum) + 노이즈 다수(Helvetica/Times-Roman/HiddenHorzOCR 뒤섞임) |
| Hidden 폰트를 걸러내면? | **참사** — 실제 데이터의 78%가 사라짐 | **정답** — needs_review 34.9%→0%, 166/166 일치 |

핵심: "Hidden"이라는 폰트 이름은 "화면에 안 보이고 선택/검색만 되는 텍스트"라는
**렌더링 속성**을 말할 뿐, 그 자체로 "진짜 데이터"인지 "노이즈"인지는 알려주지
않는다. 스캔본은 Hidden 레이어 말고 다른 게 없으니 Hidden = 본문이고, 네이티브
문서는 이미 눈에 보이는 진짜 텍스트가 있는데 그 위에 Hidden 레이어가 하나 더
겹쳐 있으니 Hidden = 중복 오염이다. **같은 신호, 반대의 결론.**

## 왜 파이프라인을 분리해야 하는가

- 필터링 기준이 "폰트 이름"이 아니라 "이 문서에 신뢰할 만한 눈에 보이는
  네이티브 텍스트 레이어가 따로 존재하는가"라는 문서 종류 자체의 성질에
  달려있다. 하나의 공용 필터로는 두 경우를 동시에 만족시킬 수 없다
  (한쪽을 고치면 다른 쪽이 깨짐).
- 그래서 v3는 필터링 로직이 없고(전체 텍스트를 그대로 신뢰), v4는
  `_is_real_content_char`(`extract_compare_v4.py`)로 `Helvetica`/
  `Times-Roman`/`Courier`/`Symbol`/`ZapfDingbats`와 이름에 `"Hidden"`이
  들어간 폰트를 걸러낸다 -- 이 함수를 v3 쪽에 절대 적용하면 안 된다.

## 체크리스트: 새 문서 종류를 파이프라인에 추가할 때

새 PDF 소스(다른 공단 문서, 다른 회사 자체 양식 등)를 붙이기 전에 반드시:

1. **`page.chars`로 폰트 분포부터 찍어본다** (`extract_words()`로 바로
   넘어가지 말 것). 최소 2페이지 이상, 정상으로 보이는 페이지와 이상하게
   보이는 페이지를 각각 하나씩 확인한다.
   ```python
   import pdfplumber
   with pdfplumber.open(path) as pdf:
       chars = pdf.pages[i].chars
       fonts = {}
       for c in chars:
           fonts[c["fontname"]] = fonts.get(c["fontname"], 0) + 1
       # 폰트별 글자 수 내림차순 출력
   ```
2. 폰트가 **하나(또는 서로 호환되는 한 계열)뿐**이면 -- 필터링 불필요,
   전체 텍스트를 그대로 신뢰(v3 방식).
3. 폰트가 **여러 개 섞여 있으면**, 각 폰트가 실제로 어느 쪽인지 확인한다:
   - 눈에 보이는 화면 내용과 대조해서, 어느 폰트의 글자들이 "읽었을 때
     말이 되는 진짜 데이터"인지 확인한다.
   - 서브셋 임베드 폰트(이름이 `XXXXXX+실제폰트명` 형태, 예:
     `LKBAPO+Dotum`)는 대개 진짜 본문일 가능성이 높다(문서 생성기가 실제
     쓴 폰트를 그대로 심은 것).
   - 이름에 `"Hidden"`이 들어간 폰트는 **그 자체로 노이즈 여부를 말해주지
     않는다** -- 위 표처럼 문서에 따라 정반대일 수 있다. 반드시 다른
     눈에 보이는 본문 레이어가 있는지부터 확인.
   - `Helvetica`/`Times-Roman`/`Courier` 같은 PDF 표준 범용 폴백 폰트가
     소량 섞여 있고 글자가 말이 안 되면(깨진 숫자, 뜬금없는 기호), 워터마크
     /도장/중복 레이어 노이즈일 가능성이 높다.
4. 노이즈로 판단되면, 그 문서 전용 필터 함수를 만들어(v4의
   `_is_real_content_char`처럼) 해당 파이프라인에만 적용한다 -- 기존
   파이프라인(v3 등)의 필터 목록에 절대 그대로 얹지 않는다.
5. 필터 적용 전/후로 `needs_review` 비율과 매칭 성공률을 실제로 비교해서
   개선됐는지 수치로 확인한다(추정하지 말 것 -- v4는 34.9%→0%로 실측
   확인함).

## 업데이트: v3/v4를 `extract_compare.py`로 통합 (2026-08-14)

위 "v3는 필터 없음 / v4는 필터 있음"으로 문서 종류별 파일을 나눴던 접근은
`extract_compare.py`(통합 버전)로 옮기면서 한 단계 더 일반화했다: 필터
적용 여부를 파일(v3/v4) 단위로 고정하는 대신, `_detect_needs_noise_filter()`
가 **PDF를 열 때마다** 첫 몇 페이지의 `page.chars` 폰트 분포를 실측해서
자동으로 켜고 끈다(임베드 폰트 비중 10% 초과면 필터링). 문서 종류(건강/연금)
가 아니라 "이 PDF가 스캔본인가 네이티브인가"가 진짜 기준이라, 같은
"health" 문서 종류라도 스캔본이 올 수도 네이티브가 올 수도 있어서
doc_type 단위 고정값으로는 안 됐다 -- 위 체크리스트의 1~3단계를 코드로
자동화한 것이라고 보면 된다.

## 알려진 한계: 국민연금(`pension`) 설정은 대아이앤씨 시트 1개로만 검증됨

`extract_compare.py`의 `_extract_excel_pension`(엑셀 쪽 국민연금 시트 파싱)은
**대아이앤씨(주기기,배관) 26.04 시트 하나로만** 실제 검증됐다(101/101 일치).
다른 회사로 확장하기 **전에 반드시 샘플 확인**할 것 -- 이유:

- 국민연금 엑셀 시트는 건강보험 시트(단순 1행 헤더: 순번/성명/건강보험료/
  장기요양보험료)와 달리 **2행짜리 그룹 헤더**를 쓴다. 실제 열 값의 의미는
  헤더 행 자체가 아니라 그 위 그룹 헤더(예: "납부보험료")에 있어서, 헤더
  탐지 로직(`_find_header_idx`, 순번/성명 위치만 봄)만으로는 그 위 그룹
  헤더까지 못 읽는다.
- 대아이앤씨는 그 달에 **하위 공사를 두 개**(플랜트배관공사/주기기설치공사)
  같이 진행해서, "사용자부담금" 열 자체가 공사별로 나뉘어 있었다(사람마다
  그중 한 열에만 값이 있고 나머지는 빈칸). `_extract_excel_pension`은 이걸
  "사용자부담금"이라는 라벨이 붙은 열을 전부 찾아서 비어있지 않은 첫 값을
  취하는 방식으로 대응했다 -- **한 사람이 같은 달에 동시에 여러 공사에서
  일하지 않는다는 전제**가 깔려 있다. 이 전제가 깨지는 회사/달이 있으면
  조용히 틀린 값(두 공사 값 중 하나만 취함)을 낼 수 있다.
- "연금보험료"(총액) 열은 헤더 행에 라벨이 아예 없어서, "사용자부담금으로
  안 잡힌 열 중 성명 바로 다음 열"이라는 위치 기반 추론으로 찾는다(코드
  주석 참고) -- 하위 공사가 하나뿐이거나 열 순서가 다른 회사 시트에서는
  이 추론이 틀릴 수 있다.

**새 회사로 pension을 확장하기 전 체크리스트**:
1. 해당 회사 엑셀 시트를 `openpyxl`로 직접 열어 헤더 2줄 구조와 "사용자
   부담금" 라벨이 몇 개 열에 붙어있는지 먼저 눈으로 확인한다.
2. `--sheet-filter`로 그 회사만 좁혀서 돌려보고, `needs_review`/매칭 결과를
   실제 PDF 값과 몇 명이라도 직접 대조한다(v4 검증 때처럼 추정하지 말 것).
3. 하위 공사가 1개뿐인 "단순" 시트와, 대아이앤씨처럼 여러 개인 "복합" 시트
   둘 다 최소 하나씩은 검증하는 걸 권장한다 -- 지금은 복합 케이스 하나만
   봤다.

## 세션 마무리 (2026-08-14)

**완성**: v3(스캔본)/v4(네이티브)를 `extract_compare.py` 하나로 통합.
health/pension 두 문서 종류를 `DOCUMENT_TYPES` 설정으로 지원하고,
숨은/노이즈 폰트 필터 적용 여부를 파일 단위 고정값이 아니라
`_detect_needs_noise_filter()`로 **PDF마다 자동 판단**하도록 개선함(위
"업데이트" 절 참고).

**검증 결과**: 대아이앤씨 267행 **100% 일치**(건강보험 166행 + 국민연금
101행, needs_review 0%).

**핵심 결론**: 공단이 직접 발급한 네이티브 텍스트 원본은 (숨은 오염
레이어만 필터링하면) **100% 정확하게 자동 대조가 가능하다**는 걸 실측으로
증명했다. 반면 스캔본은 Acrobat OCR 엔진 자체의 인식 한계(글자 누락, 앞자리
숫자 손실 등) 때문에 아무리 파이프라인을 다듬어도 `확인필요` 비율이 계속
남는다(이번 세션 거명 시트 기준 약 12%). **이 격차 자체가 "제출 방식을
스캔본 대신 공단 발급 네이티브 PDF(전자원본)로 바꿔 달라"고 요청할 근거
데이터가 된다** -- 같은 파이프라인, 같은 회사 규모에서 네이티브는 100%,
스캔은 100% 미만이라는 걸 이번 세션에서 직접 비교해 확인했다.

**한계**: `_extract_excel_pension`의 다중 하위공사 "사용자부담금" 컬럼 병합
로직이 대아이앤씨 1개 회사로만 검증됨 -- 위 "새 회사로 pension을 확장하기
전 체크리스트" 참고, 다른 회사로 확장 전 반드시 재확인 필요.

**다음 할 일 (우선순위순)**:
1. 다른 회사로 검증 범위 확대 (health/pension 둘 다 -- 특히 pension은 하위
   공사 1개뿐인 "단순" 시트로 한 번 더 검증해서 대아이앤씨 케이스가
   일반적인지 특수한지 확인). `test/` 폴더(gitignore 대상)에 26.03/26.04월
   각 13개 협력업체 PDF가 이미 있음 -- 회사마다 발급 양식(공단/공제회/자체
   서식)이 전부 달라서 health/pension 전용 파서를 회사별로 새로 진단해야
   할 가능성이 높음(2026-08-24 기준 아직 미착수).
2. 이슈 #1 검증 웹페이지 착수

## 퇴직공제부금 OCR 파이프라인 완성 (2026-08-24, `retirement_ocr.py` 신규)

**배경**: 이전 세션에서 "퇴직공제 신규 지원은 진단만 해두고 추출 로직은
없음"으로 남겨뒀던 항목을 이어서 완성했다. 처음엔 `extract_compare.py`의
기존 공용 엔진(health/pension과 같은 방식)에 `RETIREMENT_CONFIG`를 추가해
붙이려 했는데, 실제 유일한 증빙 PDF(`sample/06-05. 제20회 기성 실적정산
(퇴직공제).pdf`, 188페이지)를 열어보니 두 가지가 근본적으로 달랐다:

1. **텍스트 레이어가 없는 순수 스캔본**(전 페이지 실측: `page.chars`
   0개, 이미지만 존재) -- pdfplumber로 글자를 못 읽어 OCR이 필수.
2. **표 구조 자체가 다름** -- health/pension은 "회사x월 페이지 그룹"
   단위인데, 이 문서는 "월 1개짜리 표 안에 전체 협력업체 인원이
   업체명 컬럼으로 뒤섞여 있는" 구조(NO/성명/생년월일/근로년월/근로일수/
   직종/업체명/단위공사명/납부액, 한 달에 800~900명). 대조 대상 엑셀
   (`퇴직공제부금 납부 신고 내역(2026.05.26).xlsx`)도 시트가 회사별이
   아니라 월별('26.02'~'26.04' 등)이라 이 구조와 정확히 대응된다.

그래서 완전히 별도 모듈(`retirement_ocr.py`)로 분리했고, `extract_compare.py`
의 `DOCUMENT_TYPES` 제네릭 엔진에는 넣지 않았다(`run()`에서 doc_type이
"retirement"일 때만 `retirement_ocr.run_retirement()`으로 분기).

**OCR 구현에서 실측으로 확인한 것들**:
- 셀 하나씩 pytesseract를 부르면(9열 x 35행 = 315번) 페이지당 52초가
  걸린다(실측) -- 셀당 프로세스 기동 비용(~150ms)이 대부분. **같은 컬럼의
  모든 행을 세로로 이어붙인 이미지 1장을 컬럼당 1번만 OCR**하고 글자의
  실제 픽셀 좌표를 우리가 이미 아는 행 경계에 매핑하는 방식으로 바꾸니
  페이지당 6~7초로 줄었다(약 8배). `image_to_string`으로 줄바꿈만 세서
  행을 나누면 안 된다 -- 숫자 한 글자가 통째로 안 읽히면 그 줄이 조용히
  사라져 이후 모든 행이 한 칸씩 밀린다(실측 확인, `image_to_data`의 좌표
  매핑으로 해결 -- 실패한 칸만 빈 값이 되고 다른 행에 안 번짐).
- 표 격자선(가로/세로 검은 선) 위치는 OpenCV 없이(이 venv에 `cv2` 미설치)
  numpy 다크픽셀 합만으로 찾는다. 페이지마다 스캔 잡음으로 없던 세로선이
  잡히거나(실측: 페이지에 따라 9열인데 12개까지 잡힘) 진짜 선이 흐리게
  나오는 경우가 있어서, 실측으로 뽑은 "표 폭 대비 9열 경계 비율"
  템플릿(`TABLE_RATIOS`)에 가장 가까운 후보를 스냅하는 방식으로 안정화했다
  -- 이러면 같은 파일 안에 섞인 다른 종류 표(11열 "근로내역신고" 원본,
  공제부금납부확인서 등)도 비율이 안 맞아 자동으로 걸러진다.
- 순번(NO)열은 OCR로 안 읽는다 -- 이 표는 페이지 내내 끊김 없이 1씩
  증가하는 절대 순번이라 행 위치만으로 이미 알 수 있고, OCR에 맡기면
  오히려 오독 위험만 생겼다(실측: "1"->"4").
- 대조 매칭 키는 (성명, 생년월일, 근로년월, 단위공사명)을 쓴다. 처음엔
  업체명을 뺀 (성명,생년월일,근로년월)만 썼는데, 한 사람이 같은 달에 두
  단위공사에 걸쳐 공제부금이 나뉘는 경우가 있어서(국민연금 쪽과 같은
  문제, 위 "국민연금... 알려진 한계" 참고) outer merge가 중복 키를
  카티전 곱으로 부풀렸다(실측: 818/819행짜리 두 표를 매칭했더니 976행).
  단위공사명(짧은 영문+숫자 코드, 예 "MC-08")을 키에 추가하니 858행으로
  줄었다 -- 업체명(긴 한글 상호)보다 단위공사명이 OCR 오류가 훨씬
  적어서(실측 35행 전부 정확) 키로 쓰기에 더 안전하다.
- 근로일수/납부액 부호가 서로 다르면(예: 근로일수는 양수인데 납부액은
  음수) `needs_review`로 강제 플래그한다 -- 이 문서에서 이 둘은 항상 같은
  부호여야 정상이라(정정 건은 둘 다 음수), 부호 불일치는 OCR 오독일
  가능성이 높다(health의 "고지=납부 2배" 내부 일관성 체크와 같은 패턴).

**검증 결과**: 처음엔 26.02월 구간(24페이지, 819행)만 실측 검증했고(일치
758행=92.6%), 이후 이 PDF에 들어있는 전체 3개월분(26.02/26.03/26.04, 75
페이지, 2596행)을 실제로 끝까지 돌려 저장까지 확인했다 --
`python extract_compare.py --doc-type retirement` 또는
`python retirement_ocr.py --excel ... --pdf ...`로 재현 가능, 결과는
`result/퇴직공제_비교결과.xlsx`.

전체 3개월 합산(빨간 정정 행 보정 `_darken_red_ink` 적용 후 최종): 일치
2423행(89.0%), 엑셀만_존재 127행, 증빙만_존재 93행, 불일치 22행, 확인필요
(OCR노이즈) 21행, 과오납_의심 18행, 과소납_의심 18행(총 2722행, needs_review
비율 2.2%). 엑셀 쪽 818+905+876=2599행과 PDF 쪽 819+906+871=2596행이 거의
1:1로 맞아떨어짐 -- 스캔본 기준으로는 양호한 정확도로 판단.

**알려진 한계**:
- OCR 매칭 키가 완벽하지 않아 소수(실측 26.02월 기준 약 1.2%, 858행 중
  약 10행 초과분)의 중복 매칭이 남아있다 -- 같은 (성명,생년월일,근로년월,
  단위공사명) 키에 PDF 쪽이 여러 행을 냈거나 OCR이 서로 다른 두 사람을
  같은 값으로 잘못 읽은 경우로 추정, 아직 원인 확정은 안 함.
- "엑셀만_존재"/"증빙만_존재" 행이 꽤 있다(26.02월 기준 각 39/30행) --
  엑셀 파일 갱신일(2026.05.26)이 PDF 발급 시점보다 나중이라 이후 정정
  건이 엑셀에만 반영됐을 가능성이 있음(추정, 실제 정정 이력 대조로 확인
  필요).
- 이 PDF 안에는 형태가 다른 표(11열 "근로내역신고" 원본, 공제부금납부
  확인서)가 최소 2종류 더 섞여 있는데, 금액 컬럼이 없거나 목적이 달라
  이번 대조에는 안 씀(grid 검출 단계에서 자동으로 건너뜀).
- 이 PDF에 실제로 들어있는 건 26.02/26.03/26.04 3개월치뿐이다(188페이지 중
  표 페이지는 75개, 나머지는 표지/기성현황 요약/공제부금납부확인서/11열
  근로내역신고 원본). 엑셀은 24.08월부터 있는데 PDF(제20회 기성 실적정산)는
  최근 3개월만 담고 있어서다 -- 다음 회차(21회차 이상) PDF가 나오면 그때의
  "최근 3개월" 구간도 자동으로 뽑히긴 하겠지만(`_detect_report_month`가
  페이지 헤더에서 월을 직접 읽으므로), 그 페이지 범위·표 배치가 이번과
  똑같다는 보장은 없어 처음 받으면 반드시 위 pension 사례처럼 실제 값
  대조로 재검증할 것.
- "일치" 판정은 엑셀과 스캔본의 숫자가 같다는 뜻이지 실제 근무/납부 사실
  자체를 검증했다는 뜻은 아니다 -- 이 대조는 3-way 아키텍처 중 (2)와 (3)
  사이의 일치 여부만 확인하며, 공제회가 발급한 전체 합계 확인서(같은 PDF
  안 "건설근로자공제회 이사장" 직인 페이지)와의 대조는 아직 연결하지
  않았다.
- **"정정"(근로년월이 리포트월보다 이전인 correction) 행이 유독 부정확했다
  -- 원인을 픽셀로 확인하고 고침(`_darken_red_ink`)**: 이 문서는 정정 행을
  빨간색으로 인쇄한다. 실측(300dpi RGB 크롭): 빨간 글자의 그레이스케일
  변환값이 최대 208까지 나와(검은 글자는 훨씬 어두움) 흰 배경과 명암비가
  약해서, pytesseract 내부 이진화가 마이너스 부호나 숫자 획을 놓치는 사례가
  실제 대조 결과에서 여럿 확인됐다(예: 근로일수 -1이 1로, -13이 2로 읽힘).
  R채널이 G/B보다 확연히 높은 픽셀(빨간 글자)만 골라 검게 눌러준 뒤
  OCR하도록 고쳤다(`retirement_ocr._darken_red_ink`, 컬럼 캔버스 합성 직후
  적용) -- 같은 셀로 실측 재확인: "-1"이 이제 정확히 "-1"로, "-6,500"이
  "- 6,500"으로 읽힘. 26.02월 구간 재검증 결과 과오납_의심 8->6행,
  불일치 6->5행으로 줄었고(전체 재실행 결과는 `result/퇴직공제_비교결과.xlsx`
  참고) 실행 시간에 유의미한 영향은 없었다(페이지당 오버헤드는 컬럼당
  numpy 배열 연산 1회뿐).

## 웹 인건비 추출이 공단 확인서 서식에서 틀린 값을 내던 문제 (2026-08-26, 이슈 #5-1)

**배경**: 담당자 의견 1번("공단 PDF만 제공되니 엑셀로 export 할 수 있으면")을
점검하려고 웹 경로를 실제 샘플로 돌려봤더니, 파이프라인은 이미 다 있는데
**값 자체가 틀리고 있었다**.

`settlement_pdf_extractor.py`는 업체 자체 제출 "납부 내역서"(순번/성명/금액이
한 행) 서식을 대상으로 "한 줄에서 n번째 금액을 집는" 방식이다. 그런데 공단이
발급하는 공식 서식은 구조가 근본적으로 달라서, 이 파서가 거부하지 않고
**조용히 잘못 읽는다**(실측, `test/대아이앤씨 건강보험 납부확인서 26.04.pdf`):

- 건강보험 납부확인서는 한 사람이 3줄(성명 / 순번+고지액 / 생년월일+사용자부담)
  이라 1명이 3레코드로 쪼개지고, 금액은 "첫 금액"인 가입자부담분만 잡힌다
  (실제 납부액의 절반). 장기요양보험료 컬럼은 통째로 사라진다.
- 국민연금 결정내역서는 `_amount_index_from_header()`가 None을 내고 0으로
  폴백해서 **기준소득월액을 연금보험료로 오인**하고, 표 위 징수내역 요약의
  "이월분/전월차감" 금액까지 사람 행으로 잡는다.

**핵심**: 정확한 파서는 이미 `extract_compare.py`에 있었다(좌표 기반, 대아이앤씨
166행 100% 일치로 검증됨 -- 위 "세션 마무리" 절 참고). 다만 CLI 대조 전용이라
금액 컬럼만 읽고 성명은 엑셀 쪽에서 가져다 썼다. 즉 **정확한 엔진이 있는데 웹만
별도의 부정확한 파서를 쓰고 있던 것**이라, 새로 만들 게 아니라 연결하는 문제였다.

**한 일**: `_extract_group_records`에 `text_columns` 지원을 옵트인으로 추가
(기존 doc_type 설정에 그 키가 없으므로 CLI 결과는 그대로 -- 레코드 키까지 비교해
확인함). 웹 전용 config(성명/생년월일 좌표 + 연금 기준소득월액 컬럼)를 얹은
`confirmation_extractor.py`를 신설해, PDF 하나만으로 인당 엑셀을 만들 수 있게 했다.

실측한 컬럼 좌표(네이티브 PDF 1페이지 기준):
- 건강: 성명 108.6~135.1 / 생년월일 96.7~146.8 -> **같은 x열에 위아래로** 놓여서
  한 칸으로 읽고 내용(한글이냐 날짜냐)으로 가른다.
- 연금: 성명 70.8~100.8 / 생년월일 127.9~182.9 / 기준소득월액 230.8~275.8
  (기준소득월액은 기존 CLI 설정에 아예 없던 컬럼이라 새로 넣었다)

**검증**: 추출한 인당 금액의 합계를 PDF 하단에 공단이 직접 찍어놓은 합계와 대조
(원 단위까지 일치, needs_review 0건):

| 항목 | 추출 합계 | PDF 표기 |
|---|---|---|
| 건강보험료 | 73,802,560 | 73,802,560 |
| 장기요양보험료 | 9,652,940 | 9,652,940 |
| 연금보험료 | 57,039,360 | 57,039,360 |

**곁가지로 발견한 것**: 장기요양보험료 누락은 PDF만의 문제가 아니었다.
엑셀 경로(`_parse_premium_sheet`)도 `row[2]`(건강보험료)만 읽고 `row[3]`
(장기요양보험료)을 버리고 있었다. 건강/장기요양은 공단 고지서에서 별도 컬럼으로
부과되는 **별개 비목**이라 `longterm`을 정식 카테고리로 올렸다 -- 샘플 원장
하나에서만 7,668건이 복구됐다. 한쪽만 고쳤으면 PDF와 엑셀의 비목 구성이 서로
달라져 대조가 어긋났을 것이다.

**알려진 한계 -- 스캔본은 동기 웹 요청에서 정밀 추출을 못 한다**:
`_group_target_pages`는 텍스트 레이어가 없으면 문서 그룹을 판별하려고 페이지마다
전체 OCR을 돌린다. 실측(`sample/06-03. 제20회 기성 실적정산(건강요양).pdf`,
349페이지 순수 스캔본): **그룹 판별에만 페이지당 6.05초 -> 약 35분**. 값 추출은
그 위에 더 얹힌다. 그래서 웹에서는 `SCAN_PAGE_BUDGET`(15페이지)를 넘는 스캔본이면
정밀 경로를 건너뛰고 기존 OCR 파서에 맡기며, 결과 화면에 "공단 EDI 전자발급
원본(네이티브 PDF)을 올리면 정확히 추출된다"고 안내한다. 정밀 대조가 필요하면
CLI(`python extract_compare.py`)로 돌리면 된다 -- 이건 배치라 35분을 감당할 수 있다.
이 격차는 위 "세션 마무리" 절의 결론(네이티브 100% vs 스캔 100% 미만)과 같은
이야기이고, **제출 방식을 공단 발급 네이티브 PDF로 바꿔달라고 요청할 근거가 하나
더 늘어난 셈**이다. 웹에서도 스캔본을 정밀 처리하려면 추출을 백그라운드 작업으로
빼는 별도 작업이 필요하다(미착수).
